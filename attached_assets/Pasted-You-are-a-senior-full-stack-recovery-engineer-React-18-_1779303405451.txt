You are a senior full-stack recovery engineer, React 18 + TypeScript + Tailwind CSS expert, Node.js + Express + MySQL backend engineer, Firebase integration engineer, QA tester, UI/UX fixer, official-forms integration engineer, and university ERP workflow specialist.

Project:
Kandahar University Warehouse, Inventory & Procurement Management System

This project is already imported into Replit from GitHub.
Work directly on the current Replit project files.
Do NOT ask for ZIP.

Goal:
Fix all incomplete, broken, partial, template-data, empty-page, search, dashboard, official-form, workflow, and real-data integration problems so the system becomes truly functional.

Do NOT only create a plan.
Do NOT only audit.
Implement the fixes safely.

────────────────────────────
STRICT RULES
────────────────────────────

1. Do not rewrite the project from zero.
2. Do not remove working modules.
3. Do not destroy UI, colors, layout, sidebar, official forms, or print design.
4. Do not hardcode local paths, MySQL password, Firebase secrets, or computer-specific values.
5. Keep .env / .env.example clean.
6. Use MySQL/backend APIs for real data where available.
7. Keep localStorage fallback only as emergency/demo fallback.
8. No fake success messages.
9. No fake/template dashboard data.
10. No empty pages.
11. No broken buttons.
12. User-visible text must be Pashto/Dari only.
13. Technical abbreviations allowed only where unavoidable: ID, API, PDF, Excel.
14. Official forms must remain visually unchanged.
15. Run build tests and fix errors caused by your changes.

────────────────────────────
LOW QUOTA / REPLIT RULE
────────────────────────────

To save Replit AI quota:
1. Do not scan the whole project repeatedly.
2. Inspect only relevant files first.
3. Do not read node_modules, dist, build, .git, logs.
4. Do not rewrite files that do not need changes.
5. Implement fixes module-by-module but in one safe pass.
6. Return concise final report.

────────────────────────────
CRITICAL FIX 1 — DYNAMIC SEARCH EVERYWHERE
────────────────────────────

Search currently does not work correctly in many pages.
Fix all search boxes so they are truly dynamic.

Required:
1. Search must filter as the user types.
2. Search must not require page refresh.
3. Search must search real backend/MySQL data where backend exists.
4. If backend is unavailable, fallback to localStorage data.
5. Search must support Pashto, Dari, English letters, numbers, IDs, request tracking IDs, item names, departments, persons, categories, vendors, and statuses.
6. Search must not break when fields are missing/null.
7. Search must debounce if needed but remain fast.
8. Search result count must match visible rows.
9. Empty search result must show Pashto/Dari empty message.
10. Search must work in light/dark mode.

Fix search in:
- Requester request list
- Requester new request item search
- Request Confirmer request list
- Admin request list
- Super Admin request list
- Inventory item list
- Warehouse Entry item list
- Warehouse Director item list
- Procurement request list
- Vendor offers / comparison pages
- Receiving / delivery pages
- Reports pages
- Traceability pages
- Forecasting pages
- Users and permissions pages
- Trash/Bin page
- Notifications history

Do not add fake static search.
Search must filter real loaded data or backend query result.

────────────────────────────
CRITICAL FIX 2 — DASHBOARD CARDS, GRAPHS, REAL DATA
────────────────────────────

All dashboards must use real data, not template/demo data.

Fix:
1. General dashboard.
2. Admin dashboard.
3. Super Admin dashboard.
4. Warehouse Director dashboard.
5. Warehouse Entry dashboard.
6. Procurement Director dashboard.
7. Inventory dashboard.
8. Receiving dashboard.

Rules:
1. Every dashboard card must be clickable.
2. Clicking “Total Items” opens item list filtered to the same count.
3. Clicking “Low Stock” opens low stock item list.
4. Clicking “Out of Stock” opens out-of-stock list.
5. Clicking “Pending Requests” opens pending request list.
6. Clicking “Procurement Requests” opens procurement request list.
7. Clicking “Warehouse Requests” opens receiving/warehouse request list.
8. Clicking graph section should navigate to relevant filtered page where meaningful.
9. Counts must match the filtered destination list.
10. If no data exists, show 0 and Pashto/Dari empty state.
11. Remove template dashboard data.
12. Graphs must show real system data:
   - inventory by category
   - stock in/out
   - requests by status
   - procurement stages
   - receiving/delivery
   - low/out of stock
   - assigned items / traceability if available

────────────────────────────
CRITICAL FIX 3 — OFFICIAL FORMS HTML REPLACEMENT + BUTTONS + FORWARD-ONLY SYNC
────────────────────────────

I will provide a new official HTML file for the seven official forms.

When the new file is provided:
Replace only:
public/forms/official-forms.html

Do NOT change:
- visual design
- print layout
- table borders
- margins
- fonts
- official government structure
- CSS layout
- form width
- official graphics
- existing internal form synchronization logic unless broken
- internal form connections unless needed to restore them

The seven official forms:
1. Proposal / پیشنهاد
2. Tender / جګړه پاڼه
3. Comparison Form / فورم مقایسوي
4. Purchase Order / امر خریداری
5. Receipt Report / راپور رسید
6. SI9 / سی ۹
7. FS5 / ف س ۵

Important:
Some buttons inside official forms currently do not work.
Fix buttons safely without redesigning forms.

Fix:
1. Save buttons
2. Print buttons
3. Reset/Clear buttons
4. Add row buttons
5. Delete row buttons
6. Ribbon hide/show button
7. Font/toolbar buttons if present
8. Any top-left ribbon button
9. Any navigation/form-switch button
10. Any existing official-form action button

The ribbon hide/show button must work in all seven forms.
Event listeners must not duplicate after switching forms.
The iframe/template isolation must remain safe.

Official form viewer:
Keep or fix safely:
src/components/OfficialFormViewer.tsx

Do not convert official forms into React forms.
Do not merge all form CSS/JS globally.
Keep iframe/template isolation.

────────────────────────────
OFFICIAL FORMS FORWARD-ONLY CASCADING SYNCHRONIZATION
────────────────────────────

Restore and preserve forward-only synchronization.

Workflow order:

const formOrder = [
  "proposal",
  "tender",
  "comparison",
  "purchaseOrder",
  "receiptReport",
  "si9",
  "fs5"
];

If the project uses pc5 instead of fs5, support both safely:
const formKeyAliases = {
  pc5: "fs5",
  fs5: "fs5"
};

Core rule:
Data moves only forward.
Data must never move backward.

When a form is saved:
- that form saves its own data
- shared data goes only to downstream forms
- previous/upstream forms must not change

Mandatory protection:
Before syncing to any target form:

if (targetIndex <= sourceIndex) {
  return;
}

Required behavior:
1. Saving Proposal updates Tender, Comparison, Purchase Order, Receipt Report, SI9, FS5.
2. Saving Tender updates Comparison, Purchase Order, Receipt Report, SI9, FS5, but never Proposal.
3. Saving Comparison updates Purchase Order, Receipt Report, SI9, FS5, but never Proposal or Tender.
4. Saving Purchase Order updates Receipt Report, SI9, FS5, but never Proposal, Tender, or Comparison.
5. Saving Receipt Report updates SI9 and FS5 only.
6. Saving SI9 updates FS5 only.
7. Saving FS5 saves itself only and never updates previous forms.

Example:
Proposal quantity = 4 and save:
- Proposal remains 4
- all downstream forms become 4

Tender changes quantity to 3 and save:
- Proposal remains 4
- Tender becomes 3
- Comparison, Purchase Order, Receipt Report, SI9, FS5 become 3

Comparison changes quantity to 1 and save:
- Proposal remains 4
- Tender remains 3
- Comparison becomes 1
- Purchase Order, Receipt Report, SI9, FS5 become 1

Shared fields to sync forward:
- serial number / شمار
- item name / اسم جنس
- item type/specification / اسم نوعیت / مشخصات
- quantity / مقدار
- unit price / قیمت دانه
- unit / واحد
- total price / قیمت مجموعي
- grand total
- subject
- requester information
- faculty
- department
- person/receiver information
- request ID / tracking ID where applicable

Calculation:
قیمت مجموعي = مقدار × قیمت دانه

When quantity or unit price changes:
1. total price updates automatically
2. grand total updates automatically if present
3. calculation must support empty values as 0
4. Pashto/Dari digits must not crash
5. decimal numbers must work
6. Comparison Form calculation must work correctly

Private fields must NOT be overwritten:
- signatures
- approval notes
- decision fields
- comments
- dates that belong only to that form
- stamps
- officer comments
- director comments
- confirmation notes
- handwritten notes
- local status
- form-specific fields

Each form must have its own saved data object.

Do not wipe existing localStorage form data unless replacing only the correct form key.
If old keys exist, support them safely.

Required central functions if not already present:
- getFormOrder()
- getFormIndex(formName)
- normalizeFormKey(formName)
- getStorageKey(formName)
- collectFormData(formName)
- collectSharedSyncData(formName)
- collectSharedItemRows(formName)
- calculateRowTotal(row)
- calculateGrandTotal(rows)
- saveFormData(formName, data)
- loadFormData(formName)
- getDownstreamForms(formName)
- preventBackwardSync(sourceFormName, targetFormName)
- syncForwardOnly(sourceFormName, sharedData)
- applySharedDataToForm(sourceFormName, targetFormName, sharedData)

Do not rewrite official forms if a safe existing sync controller already exists.
Repair it minimally.

────────────────────────────
CRITICAL FIX 4 — REQUESTER + CUSTOM ITEM REQUEST
────────────────────────────

Requester new request currently has only dropdown lists.
Fix it.

Requester must be able to request:
1. Existing item from dropdown/search
2. Custom/new item not existing in item list

Required fields:
- item name
- specification/type
- quantity
- unit
- faculty
- department
- person/receiver
- reason
- request level
- current status
- progress percentage

Request cannot submit without request level.
Request details must not mix data with another request.
Request list must show correct request data.

────────────────────────────
CRITICAL FIX 5 — ROLE LEVEL USERS + RBAC + PROGRESS TRACKING
────────────────────────────

Most important system pillars:
1. Progress Tracking
2. Role-level users
3. Correct data ownership
4. Correct pipeline
5. Correct audit trail

Fix:
1. Role-based dashboards.
2. Role-based sidebar menus.
3. Requester sees own requests only unless allowed.
4. Confirmer sees related requests only.
5. Procurement Director sees procurement requests only.
6. Warehouse Director sees receiving/delivery requests.
7. Warehouse Entry Person sees entry-related pages only.
8. Admin and Super Admin see allowed full modules.
9. No user should see or edit unrelated data.
10. No request data should be saved under the wrong request.
11. No item assignment should be saved under the wrong person.
12. No procurement data should mix between requests.

Progress tracking must save:
- request id
- stage
- old status
- new status
- role
- user
- request level
- comment/reason
- timestamp
- progress percent
- audit log

Progress percent must update based on real workflow status.

────────────────────────────
CRITICAL FIX 6 — INVENTORY, ENTRY, STOCK IN/OUT
────────────────────────────

Fix all inventory problems:
1. Entry section / ګدام داخل must work.
2. Stock-in button must work.
3. Stock-out button must work.
4. Edit button must work.
5. Ledger button must work.
6. Action buttons must exist and work for Admin, Super Admin, Warehouse Director, Warehouse Entry Person where allowed.
7. Add item dropdowns must support keyboard typing/search.
8. Duplicate item with same specification must be blocked.
9. Stock-in must increase MySQL current_stock.
10. Stock-out must decrease MySQL current_stock.
11. Negative stock must be blocked.
12. All transactions must be written to stock_transactions.
13. Delivered items must appear in traceability/person assignment.

────────────────────────────
CRITICAL FIX 7 — PROCUREMENT, RECEIVING, DELIVERY
────────────────────────────

Procurement:
1. Procurement dashboard clickable.
2. Procurement request list must show real sent-to-procurement requests.
3. Action buttons must work.
4. Three vendor prices must work.
5. Comparison must work.
6. 3-vendor rule must be enforced.
7. Winner selection must work.
8. Purchase order must work.
9. No template data.
10. No mixed request/procurement data.

Receiving:
1. Warehouse Director dashboard must work.
2. Receipt Report management must be button/tab based.
3. FS5 management must be button/tab based.
4. Receipt must increase stock.
5. FS5 delivery must decrease stock.
6. Duplicate deduction must be prevented.
7. Delivery must assign items to person/department/faculty.
8. Action buttons must be visible in dark/light mode.

────────────────────────────
CRITICAL FIX 8 — NOTIFICATIONS
────────────────────────────

Fix notification module:
1. Sending one notification must create/send only one record.
2. Prevent duplicate notification sends.
3. Disable button while sending.
4. Add idempotency key if needed.
5. Notification history must not duplicate.
6. If Firebase/email/SMS config is missing, show clear Pashto/Dari message.
7. Do not fake successful send.
8. Replace wrong/template images if present.
9. Admin incoming goods notification must work if supported.

────────────────────────────
CRITICAL FIX 9 — REPORTS, TRACEABILITY, FORECASTING
────────────────────────────

Reports:
1. Print must work.
2. Excel/CSV export must work or show clear real limitation.
3. Reports must use real backend/MySQL data.
4. No template data.
5. Daily/monthly/yearly reports where possible.
6. Report cards clickable.
7. Fix unreadable hover/button text.

Traceability:
Create/fix Traceability menu with grid layout.

Two sections:
1. Administrative section
2. Faculties section

Each section must show counts.

Faculty section:
- Bachelor
- Master
- PhD
- Departments
- People
- Person ledger
- Assigned items

Administrative section:
- Departments
- People
- Person ledger
- Assigned items

Traceability filters:
- all
- administrative
- faculties
- level
- faculty
- department
- person
- item
- date range
- request id

Traceability print/export:
- all traceability
- faculty section only
- administrative section only
- selected faculty
- selected department
- selected person

Counts must roll up:
person → department → faculty/level → section total.

Forecasting:
Create/fix Forecasting menu:
1. last 12 months
2. last 6 months
3. last 3 months
4. faculty/department needs
5. urgent / medium / low / enough
6. recommended purchase quantity
7. print
8. Excel
9. use real OUT/consumption data
10. no fake data

────────────────────────────
CRITICAL FIX 10 — SETTINGS, USERS, TRASH/BIN, ABOUT PAGE
────────────────────────────

Fix:
1. About us / زموږ په اړه must work everywhere.
2. Settings last review must be Pashto/Dari.
3. Users and permissions must work.
4. User image must appear beside name and in account dropdown.
5. Remove unsafe Firebase secret display from Super Admin settings.
6. Trash/Bin must exist.
7. Delete should be soft delete where possible.
8. Restore must work.
9. Permanent delete only for Super Admin if implemented.
10. Trash must include deleted records where possible.

────────────────────────────
CRITICAL FIX 11 — UI, SIDEBAR, ANIMATION, DARK MODE, MOBILE
────────────────────────────

Sidebar:
1. Improve sidebar UI consistently.
2. Keep role-based menus accurate.
3. No empty page routes.
4. No broken menu items.

Animated UI:
1. Add lightweight animation where safe.
2. Do not make system heavy.
3. Use Tailwind-friendly transitions.

Dark/Light mode:
1. Fix invisible buttons/text.
2. No white text on white background.
3. No black text on black background.
4. Fix reports, receiving actions, procurement actions.

Mobile/PWA:
1. Ensure responsive layout.
2. Add SETUP.md mobile/PWA steps:
   - responsive web
   - install as PWA
   - optional Capacitor APK path if safe
3. Do not rewrite into Flutter.
4. Replit mobile preview should not break.

────────────────────────────
FINAL TESTS
────────────────────────────

Run:
Frontend:
npm install
npm run build
npm run dev

Backend:
cd backend
npm install
npm run build
npm run dev

Test:
1. Login works.
2. Forgot password safe.
3. Dashboard cards clickable and counts match filtered pages.
4. Graphs use real data.
5. Search works dynamically.
6. Add item works.
7. Stock in works.
8. Stock out works.
9. Negative stock blocked.
10. Request with existing item works.
11. Request with custom item works.
12. Request list correct.
13. Confirmer search/list correct.
14. Progress tracking works.
15. Procurement workflow works.
16. Three offers + comparison + winner works.
17. Purchase order works.
18. Receiving increases stock.
19. FS5 delivery decreases stock once.
20. Item assignment appears in traceability.
21. Notifications do not duplicate.
22. Reports print/export work.
23. Official forms open.
24. Official forms buttons work.
25. Ribbon hide/show works.
26. Forward-only sync works.
27. Trash restore works.
28. About page works.
29. Dark/light mode visible.
30. System works on another computer after setup.

────────────────────────────
FINAL REPORT REQUIRED
────────────────────────────

Return a concise final report:
1. Files changed.
2. What was broken.
3. What was fixed.
4. Dynamic search status.
5. Dashboard clickable cards status.
6. Real data vs template data status.
7. Official forms replacement status.
8. Official forms button/ribbon status.
9. Forward-only sync status.
10. Inventory status.
11. Requests/progress/RBAC status.
12. Procurement status.
13. Receiving/delivery status.
14. Notifications duplicate bug status.
15. Reports/traceability/forecasting status.
16. Trash/settings/users/about status.
17. Build results.
18. Test results.
19. Remaining limitations.
20. Setup steps for another computer.

Do not stop at planning.
Implement the fixes now.